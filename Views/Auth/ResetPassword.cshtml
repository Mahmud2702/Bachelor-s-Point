@model ResetPasswordDto
@{
    ViewData["Title"] = "Reset Password";
    bool allowResend = ViewBag.AllowResend is bool b && b;
}

<div class="auth-card" style="max-width: 520px;">
    <div class="text-center mb-4">
        <div style="width: 80px; height: 80px; background: var(--clr-warning-light); color: var(--clr-warning); border-radius: 50%; display: inline-flex; align-items: center; justify-content: center; font-size: 2.2rem; margin-bottom: 1rem;">
            <i class="bi bi-shield-lock-fill"></i>
        </div>
        <h2 style="font-size: 1.45rem; font-weight: 800; margin-bottom: 0.5rem;">Reset your password</h2>
        <p style="color: var(--clr-muted); font-size: 0.9rem; margin: 0;">
            Enter the 6-digit code sent to<br />
            <strong style="color: var(--clr-text);">@Model.Email</strong>
        </p>
    </div>

    <form asp-action="ResetPassword" method="post" autocomplete="off">
        @Html.AntiForgeryToken()
        <div asp-validation-summary="ModelOnly" class="text-danger small mb-2"></div>

        <input asp-for="Email" type="hidden" />

        <div class="mb-3">
            <label asp-for="Otp" class="form-label">Verification Code</label>
            <input asp-for="Otp" class="form-control text-center"
                   style="font-size: 1.55rem; letter-spacing: 0.7rem; font-weight: 700; padding: 0.75rem 0.5rem; font-family: 'Plus Jakarta Sans', monospace;"
                   inputmode="numeric" pattern="\d{6}" maxlength="6"
                   placeholder="——————" autofocus />
            <span asp-validation-for="Otp" class="text-danger small"></span>
        </div>

        <div class="mb-3">
            <label asp-for="NewPassword" class="form-label">New Password</label>
            <div style="position: relative;">
                <i class="bi bi-lock" style="position: absolute; left: 12px; top: 50%; transform: translateY(-50%); color: var(--clr-muted);"></i>
                <input asp-for="NewPassword" type="password" class="form-control" style="padding-left: 38px;" placeholder="At least 8 characters" />
            </div>
            <span asp-validation-for="NewPassword" class="text-danger small"></span>
        </div>

        <div class="mb-3">
            <label asp-for="ConfirmPassword" class="form-label">Confirm New Password</label>
            <div style="position: relative;">
                <i class="bi bi-lock-fill" style="position: absolute; left: 12px; top: 50%; transform: translateY(-50%); color: var(--clr-muted);"></i>
                <input asp-for="ConfirmPassword" type="password" class="form-control" style="padding-left: 38px;" placeholder="Re-enter new password" />
            </div>
            <span asp-validation-for="ConfirmPassword" class="text-danger small"></span>
        </div>

        <button type="submit" class="btn btn-primary w-100 mb-2" style="height: 46px;">
            <i class="bi bi-check-circle me-1"></i> Reset Password
        </button>

        <p class="text-center mb-0" style="color: var(--clr-muted); font-size: 0.82rem;">
            Code expires in <strong style="color: var(--clr-text);">5 minutes</strong>.
        </p>
    </form>

    <hr style="border-color: var(--clr-border); margin: 1.5rem 0;" />

    <div class="text-center">
        <p style="color: var(--clr-muted); font-size: 0.85rem; margin-bottom: 0.65rem;">
            Didn't receive the code?
            @if (allowResend)
            {
                <br /><span style="color: var(--clr-danger);">Your code expired — please request a new one.</span>
            }
        </p>
        <form asp-action="ResendResetOtp" method="post" class="d-inline">
            @Html.AntiForgeryToken()
            <input type="hidden" name="email" value="@Model.Email" />
            <button type="submit" class="btn btn-outline-secondary btn-sm">
                <i class="bi bi-arrow-clockwise me-1"></i> Resend Code
            </button>
        </form>
        <a asp-action="ForgotPassword" class="btn btn-sm" style="color: var(--clr-primary); text-decoration: none;">
            <i class="bi bi-pencil me-1"></i> Change email
        </a>
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
    <script>
        (function() {
            const input = document.querySelector('input[name="Otp"]');
            if (input) {
                input.addEventListener('input', () => {
                    input.value = input.value.replace(/\D/g, '').slice(0, 6);
                });
            }
        })();
    </script>
}
